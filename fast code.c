#include<stdio.h>
int main(){
    int a=5;
    float b=5.9;

    printf("size of the int: %zu\n",sizeof(a));
        printf("size of the int: %zu \n",sizeof(b));
        printf("size of the int: %zu",sizeof(char));

    return 0;

}
